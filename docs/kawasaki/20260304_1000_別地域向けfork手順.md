# 別地域向け Fork 手順書

> このガイドは、川崎市議会版（みらい議会ー川崎版）を別の地方議会向けに Fork する際の手順をまとめたものです。
> 神奈川県議会版（2026-03 Fork 実施）の経験を反映しています。

---

## 1. はじめに

本リポジトリはオープンソースで公開されており、他の地方議会向けにカスタマイズして利用できます。
地域固有の情報は `site.config.ts` に集約されているため、**最小限の変更でForkが可能**です。

ただし `site.config.ts` 以外にもハードコードされた地域参照（UI テキスト・コメント・シードデータ等）があるため、セクション 3 の確認が必須です。

---

## 2. 最低限変更が必要なファイル

### 2-1. `web/src/config/site.config.ts` ← **最重要・ここだけ変えればほぼ動く**

```typescript
export const siteConfig = {
  siteName: "みらい議会ーXX版",          // ← サイト名を変更
  siteDescription: "XX議会で今どんな議案が検討されているか...",  // ← 説明を変更
  cityName: "XX市",                       // ← 自治体名を変更（県の場合は "XX県"）
  councilName: "XX市議会",               // ← 議会名を変更（県の場合は "XX県議会"）
  keywords: ["みらい議会ーXX版", "議案", "XX市", ...],  // ← キーワードを変更
  councilBaseUrl: "https://www.city.xx.jp/",             // ← 議会サイトのベースURLを変更
  councilBillsDetailUrl: "https://www.city.xx.jp/...",   // ← 議案・議決結果一覧URLを変更
  twitterHashtag: "みらい議会XX版",      // ← Xのハッシュタグを変更（#なし）
  externalLinks: {
    report: "https://forms.gle/...",       // ← 問題報告フォームURLを変更
    aboutNote: "https://note.com/...",     // ← サービス説明noteのURLを変更
    donation: "https://team-mir.ai/support/donation",  // ← 変更不要（チームみらい共通）
    teamAbout: "https://team-mir.ai/about",            // ← 変更不要（チームみらい共通）
    terms: "https://team-mir.ai/terms",                // ← 変更不要（チームみらい共通）
    privacy: "https://team-mir.ai/privacy",            // ← 変更不要（チームみらい共通）
    faq: "https://team-mirai.notion.site/FAQ-...",     // ← 変更不要（チームみらい共通）
  },
  /**
   * ページを管理する政党名（空文字列の場合は政党名を省略した汎用表現を使用）
   * 例: "チームみらい"
   */
  managingParty: "",
  /**
   * サービス運営者情報
   * 利用規約や問い合わせ先に使用します。
   */
  operator: {
    name: "運営者名",                      // ← 運営者名を変更
    contactUrl: "https://...",             // ← 問い合わせ先URLを変更
    jurisdiction: "XX地方裁判所",          // ← 管轄裁判所を変更
  },
  features: {
    aiChat: true,                          // ← AIチャット機能のオン/オフ
    aiInterview: false,                    // ← AIインタビュー機能のオン/オフ
    showTeamMiraiSection: false,           // ← チームみらいセクション表示（非公式運営ならfalse）
  },
} as const;
```

### 2-2. `admin/src/config/site.config.ts`

```typescript
export const siteConfig = {
  siteName: "みらい議会ーXX版",
  cityName: "XX市",
  councilName: "XX市議会",
  councilBaseUrl: "https://www.city.xx.jp/",
  councilBillsDetailUrl: "https://www.city.xx.jp/...",
  councilFactionExamples: "みらいXX市議会、XX市自由民主党議員団、XX市公明党議員団等",
} as const;
```

### 2-3. `web/public/manifest.json`（JSONのためTSインポート不可・手動編集）

```json
{
  "name": "みらい議会ーXX版",
  "short_name": "みらい議会",
  ...
}
```

### 2-4. `packages/seed/main/data.ts`（テストデータ）

定例会・会派・委員会のシードデータを差し替えてください。

```typescript
// 定例会の例
export const COUNCIL_SESSIONS = [
  {
    name: "令和XX年 第X回定例会",
    slug: "XXX-teireikai",
    start_date: "XXXX-XX-XX",
    end_date: "XXXX-XX-XX",
    is_active: true,
  },
];

// 会派の例
export const FACTIONS = [
  { name: "みらいXX市議会" },
  { name: "XX市自由民主党議員団" },
  ...
];
```

### 2-5. `packages/seed/main/bill-contents-data.ts`（議案コンテンツ詳細データ）

議案の詳細コンテンツ（markdown形式の説明文・法律文書テンプレート）に地域固有の文言が大量に含まれています。**全ファイルの置換が必要**です。

主な置換パターン:

| 置換元 | 置換先の例 | 備考 |
|---|---|---|
| 自治体名（「川崎市」） | Fork先の自治体名 | |
| 条例番号（「平成X年川崎市条例第XX号」） | Fork先の条例番号 | |
| 行政機関（「市役所」「市立」） | 「県庁」「県立」等 | 県議会向けの場合 |
| 住民呼称（「市民」） | 「県民」等 | 県議会向けの場合 |
| 財源（「市税」） | 「県税」等 | 県議会向けの場合 |
| 地域固有情報（区名・地区名等） | Fork先の地域情報 | |

---

## 3. ハードコードされた地域参照の修正

`site.config.ts` 以外にも、UI テキストやコメントに地域名がハードコードされています。
Fork 時は以下を確認・修正してください。

### 3-1. UI表示に影響するもの（必ず修正）

| ファイル | 内容 |
|---|---|
| `web/src/features/council-sessions/client/components/council-session-bill-list.tsx` | "川崎市議会情報へ" リンクラベル |
| `web/src/features/chat/client/components/chat-window.tsx` | "みらい議会ー川崎版って何？" サジェスト |
| `web/src/features/chat/client/components/chat-window.tsx` | "市議会って何をするところ？" サジェスト（県議会等の場合は変更） |
| `web/src/features/interview-report/client/components/make-public-modal.tsx` | 同意テキスト内の "みらい議会ー川崎版" |
| `web/src/features/interview-report/client/components/interview-public-consent-modal.tsx` | 同意テキスト内の "みらい議会ー川崎版" |
| `admin/src/features/ai-collection/server/utils/build-prompt.ts` | AI生成プロンプト内の会派名例 |
| `admin/src/features/ai-collection/server/utils/build-status-check-prompt.ts` | 同上 |

### 3-2. コメントのみ（動作に影響なし、修正推奨）

| ファイル | 内容 |
|---|---|
| `web/src/features/bills/client/components/bill-detail/bill-status-progress.tsx` | "川崎市議会: 一院制" |
| `web/src/features/bills/server/repositories/bill-repository.ts` | "川崎市議会DB" |
| `web/src/features/bills/shared/types/index.ts` | "川崎DB" |
| `web/src/features/bills/shared/utils/bill-progress.ts` | "川崎市議会" |
| `web/src/components/top/coming-soon-section.tsx` | 川崎市議会情報リンク |
| `admin/src/features/ai-collection/server/utils/faction-matching.ts` | "川崎市議会議員団" |

### 3-3. 「市議会」「市民」等の市レベル固有表現（県議会等にForkする場合）

市議会→県議会のように**議会レベルが変わる場合**は、「川崎」以外に「市議会」「市民」等の表現も置換が必要です。

```bash
# 「市議会」の検索（「県議会」等に置換が必要な箇所を特定）
grep -r "市議会" web/src admin/src --include="*.ts" --include="*.tsx" | grep -v site.config | grep -v node_modules

# 「市民」の検索（UI表示テキストは確認が必要）
grep -r "市民" web/src admin/src --include="*.ts" --include="*.tsx" | grep -v node_modules
```

| 表現 | 対応方針 |
|---|---|
| `市議会` → UI表示テキスト | `siteConfig.councilName` を使用するか、「議会」に汎用化 |
| `市議会` → admin UIラベル（「市議会URL」等） | 「議会URL」に汎用化 |
| `市民` → AIプロンプト・インタビュー設定内 | 広義の「市民（citizen）」として残してよい場合が多い |
| `市民` → UI表示テキスト（「市民の皆さまが」等） | 「皆さまが」に汎用化するか、地域呼称に置換 |

主な対象ファイル:

**web/src:**
- `features/interview-config/client/components/interview-lp-page.tsx` - 「市議会での審議に」
- `app/faq/page.tsx` - 「市民の皆さまが」
- `features/bills/client/components/share/bill-share-modal.tsx` - 「市議会の議論を」
- `app/(main)/terms/page.tsx` - 「市議会議案等」
- `components/top/about.tsx` - 「市議会での議論を」「市民の意見を」
- `components/top/team-mirai.tsx` - 「市民に届ける」
- `components/layouts/desktop-menu/logo.tsx` - 「市議会の議論を」
- `features/bills/server/components/featured-bill-section.tsx` - 「市議会に上程された」
- `features/chat/client/components/chat-window.tsx` - 「市議会って何をするところ？」

**admin/src:**
- `features/council-sessions/client/components/council-session-item.tsx` - 「市議会URL」「市議会ページ」
- `features/council-sessions/client/components/council-session-form.tsx` - 「市議会URL」

---

## 4. CLAUDE.md の更新

Fork 先リポジトリの `CLAUDE.md` にも川崎固有の設定が含まれています。以下を確認・更新してください：

- **Worktree パス**: `mirai-gikai-kawasaki-worktree` → Fork 先に合わせたパス名に変更
- **PR ベースブランチ**: `kawasaki/develop` → Fork 先のベースブランチに変更（例: `kanagawa/develop`）

---

## 5. GitHub Actions の更新

`.github/workflows/` 配下のワークフローにブランチフィルタが設定されています。Fork 先のブランチ名に合わせて変更してください。

| ファイル | 変更箇所 |
|---|---|
| `.github/workflows/code_check.yml` | `branches:` のフィルタ（例: `kawasaki/develop` → `kanagawa/develop`） |
| `.github/workflows/deploy_web.yml` | `branches:` のフィルタおよび `ref:` のチェックアウト先 |

---

## 6. 画像素材の差し替え

| ファイル | 用途 | サイズ |
|---|---|---|
| `web/public/ogp.jpg` | OGP画像（SNSシェア時） | 1200×630px |
| `web/public/img/logo.svg` | サイトロゴ | SVG |
| `web/public/img/hero_background.png` | トップページヒーロー背景 | 任意 |
| `web/public/icons/pwa/icon_android_192.png` | PWAアイコン（Android） | 192×192px |
| `web/public/icons/pwa/icon_ios.png` | PWAアイコン（iOS） | 180×180px |

---

## 7. 環境変数の設定

`.env.example` を `.env` にコピーして値を設定してください。

> **注意**: `.env.example` が存在しない場合は、川崎版の `.env.example` を参考に手動で `.env` を作成してください。

```bash
cp .env.example .env
```

主要な設定項目：

| 変数名 | 説明 | 必須 |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | SupabaseプロジェクトURL | ✅ |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase匿名キー | ✅ |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabaseサービスロールキー | ✅ |
| `SUPABASE_URL` | Supabase URL（サーバーサイド用） | ✅ |
| `REVALIDATE_SECRET` | Web側キャッシュ無効化用シークレットキー | ✅ |
| `AI_GATEWAY_API_KEY` | Vercel AI GatewayのAPIキー（AIチャット・インタビュー機能に必要） | ✅ |
| `NEXT_PUBLIC_WEB_URL` | 公開WebアプリのURL | ✅ |
| `ADMIN_URL` | AdminサーバーのURL | ✅ |
| `OPENAI_API_KEY` | OpenAI APIキー（DALL-Eによる議案サムネイル画像の自動生成に使用） | ✅ |
| `DALLE_MODEL` | DALL-Eモデル選択（`dall-e-2` または `dall-e-3`、省略時は `dall-e-3`） | 任意 |
| `LANGFUSE_PUBLIC_KEY` | Langfuse公開キー（プロンプト管理・LLM監視） | ✅ |
| `LANGFUSE_SECRET_KEY` | Langfuseシークレットキー | ✅ |
| `LANGFUSE_BASE_URL` | LangfuseベースURL（デフォルト: `https://cloud.langfuse.com`） | ✅ |
| `CHAT_DAILY_USER_COST_LIMIT_USD` | AIチャット日次コストリミット（USD）- ユーザー単位 | 任意 |
| `CHAT_DAILY_TOTAL_COST_LIMIT_USD` | AIチャット日次コストリミット（USD）- システム全体 | 任意 |
| `CHAT_MONTHLY_TOTAL_COST_LIMIT_USD` | AIチャット月間コストリミット（USD）- システム全体 | 任意 |
| `CLAUDE_CLI_PATH` | AI情報収集で使用するClaude CLIのパス（省略時はPATH上の `claude`） | 任意 |
| `BASIC_AUTH_USER` / `BASIC_AUTH_PASSWORD` | Basic認証（ステージング環境等で利用） | 任意 |
| `WEB_PORT` / `ADMIN_PORT` | devサーバーのポート番号（デフォルト: 3002 / 3001）。複数のmirai-gikai系プロジェクトを同時起動する場合に変更 | 任意 |
| `NEXT_PUBLIC_GA_TRACKING_ID` | Google Analytics ID | 任意 |

### 7-1. OpenAI API キーの取得

議案サムネイル画像の自動生成（DALL-E）に OpenAI API キーが必要です。

1. [OpenAI Platform](https://platform.openai.com/) でアカウントを作成
2. API Keys ページで新しいキーを発行
3. `.env` の `OPENAI_API_KEY` に設定
4. DALL-E の利用には OpenAI アカウントへのクレジット追加（従量課金）が必要です

---

## 8. Supabase・DBセットアップ手順

```bash
# ローカルSupabaseの起動
npx supabase start

# マイグレーションの適用
pnpm db:migrate

# 型定義の生成
pnpm db:types:gen

# シードデータの投入
pnpm seed
```

詳細は `docs/kawasaki/20260219_1000_ローカル環境起動手順.md` を参照してください。

---

## 9. Langfuse プロンプト設定

AIチャット・インタビュー機能では Langfuse でプロンプトを管理しています。

1. Langfuse のプロジェクトを作成し、環境変数を設定
2. 以下のプロンプトを議会・地域情報に合わせて更新：

| プロンプト名 | 用途 | 地域カスタマイズ |
|---|---|---|
| `top-chat-system` | トップページAIチャット | 議会名・地域名の反映 |
| `bill-chat-system-normal` | 議案詳細チャット（通常難易度） | 議会名の反映 |
| `bill-chat-system-easy` | 議案詳細チャット（やさしい） | 同上 |
| `bill-chat-system-hard` | 議案詳細チャット（詳しい） | 同上 |

3. 更新後は管理画面で動作確認

> **注意**: インタビュー機能のプロンプトは現在コード内で動的生成されており、Langfuse では管理されていない。地域固有の文言がある場合はコード側も確認が必要。

---

## 10. クロスプラットフォーム対応（Windows / Mac）

本リポジトリは Mac・Windows 両方での開発に対応しています。以下の点に注意してください。

### 10-1. devスクリプトのポート指定

`web/package.json` と `admin/package.json` の dev スクリプトは、Bash の `${VAR:-default}` 構文ではなく Node.js の `process.env` 経由でポートを読み取る方式になっています。**この方式は Windows でも動作するため、変更不要です。**

もし独自にスクリプトを追加する場合は、Bash 構文（`${VAR:-default}`）を避け、Node.js または `cross-env` 等のクロスプラットフォームツールを使用してください。

### 10-2. Claude CLI（AI情報収集機能）の Windows 対応

`admin/src/features/ai-collection/server/utils/execute-claude.ts` に Windows 対応が組み込まれています：

| 対応項目 | 内容 |
|---|---|
| CLI パス解決 | Windows では `%APPDATA%/npm/claude.cmd` を自動検出 |
| shell オプション | Windows では `.cmd` ラッパー実行のため `shell: true` を自動設定 |
| プロンプト受け渡し | コマンドライン引数ではなく stdin 経由（Windows のコマンド長制限を回避） |

Windows で AI 情報収集機能を使う場合は、`CLAUDE_CLI_PATH` 環境変数に Claude CLI のフルパスを設定するか、npm グローバルインストール（`npm install -g @anthropic-ai/claude-code`）を行ってください。

### 10-3. Supabase CLI

`npx supabase start` は Docker を使用します。Windows の場合は WSL2 + Docker Desktop が必要です。Mac では Docker Desktop または OrbStack 等を使用してください。

### 10-4. 改行コード

Biome が LF を強制するため、Windows で `git clone` する際は改行コードの自動変換を無効にすることを推奨します：

```bash
git config core.autocrlf input
```

---

## 11. 推奨作業順序

```
1. CLAUDE.md のブランチ名・worktreeパス更新（セクション4）
   ↓
2. GitHub Actions のブランチフィルタ更新（セクション5）
   ↓
3. site.config.ts × 2 + manifest.json の更新（セクション2）
   ↓
4. ハードコード地域参照の修正（セクション3）
   ↓
5. pnpm typecheck && pnpm lint && pnpm build で動作確認
   ↓
6. シードデータ差し替え（セクション2-4, 2-5）※ 議会情報の調査が前提
   ↓
7. 画像素材差し替え（セクション6）※ デザイン作成が前提
   ↓
8. Langfuse プロンプト設定（セクション9）
   ↓
9. 環境変数・インフラセットアップ（セクション7）
   ↓
10. クロスプラットフォーム確認（セクション10）※ Windows で開発する場合
   ↓
11. リリース前チェックリスト実施（セクション12）
```

---

## 12. リリース前チェックリスト

### 設定ファイル
- [ ] `web/src/config/site.config.ts` の全項目を確認・更新
- [ ] `admin/src/config/site.config.ts` の全項目を確認・更新
- [ ] `web/public/manifest.json` を更新

### 画像・素材
- [ ] OGP画像（`web/public/ogp.jpg`）を差し替え
- [ ] ロゴ・アイコン類を差し替え

### データ・プロンプト
- [ ] シードデータを差し替えて `pnpm seed` 実行
- [ ] Langfuse プロンプトを更新（4種）
- [ ] OpenAI API キーを設定し、議案サムネイル画像の自動生成が動作することを確認

### CI/CD・設定
- [ ] CLAUDE.md のパス・ブランチ名がFork先向けであることを確認
- [ ] GitHub Actions のブランチフィルタがFork先向けであることを確認

### 地域参照の漏れチェック
- [ ] `grep -r "川崎" web/src admin/src packages/seed --include="*.ts" --include="*.tsx" | grep -v site.config | grep -v node_modules` で漏れがないことを確認
- [ ] 議会レベルが変わる場合: `grep -r "市議会" web/src admin/src --include="*.ts" --include="*.tsx" | grep -v site.config` で漏れがないことを確認

### クロスプラットフォーム（Windows で開発する場合）
- [ ] `git config core.autocrlf input` で改行コード自動変換を無効化
- [ ] Docker Desktop（WSL2）がインストール済みであることを確認
- [ ] Claude CLI が Windows で動作することを確認（`CLAUDE_CLI_PATH` 設定 or npm グローバルインストール）

### ビルド・テスト
- [ ] `pnpm typecheck` でエラーがないことを確認
- [ ] `pnpm lint` でエラーがないことを確認
- [ ] `pnpm build` でビルドが通ることを確認
- [ ] `pnpm test` で全テスト通過を確認

### 表示確認
- [ ] `pnpm dev` でローカル起動し、各ページの表示を確認
  - [ ] トップページ（サイト名・説明・外部リンク）
  - [ ] 議案詳細ページ（免責事項・SNSシェア）
  - [ ] Admin管理画面（タイトル・プレースホルダー）
- [ ] 各ページの OGP プレビューを確認（[OGP確認ツール](https://ogp.biz/)等）
- [ ] SNS シェアのハッシュタグを確認
