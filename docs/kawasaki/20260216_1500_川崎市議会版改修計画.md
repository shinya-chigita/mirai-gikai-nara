# みらい議会 → みらい議会ー川崎版 改修計画

**作成日:** 2026-02-16
**ステータス:** Phase 4 完了・本番公開準備完了

## Context

現在の「みらい議会」は国会（衆議院・参議院の二院制）の法案をわかりやすくまとめ、AI Chatで質問できるサービス。
これを Fork して川崎市議会（一院制の地方議会）版として「みらい議会ー川崎版」を作成する。

### 主な差異

| 項目 | 国会（現在） | 川崎市議会（変更後） |
|------|-------------|---------------------|
| 議会構造 | 二院制（衆議院+参議院） | 一院制 |
| 議案呼称 | 法案 | 議案 |
| 会期 | 第XXX回国会（通常会/臨時会） | 令和X年 第X回定例会 / 臨時会 |
| 審議の流れ | 提出→発議院→送付院→成立 | 上程→委員会付託→委員会審査→本会議採決 |
| 組織 | 衆議院/参議院 | 委員会制度（総務、文教等） |
| 見解表示 | チームみらい1会派のみ | 複数会派の見解を登録可能 |

---

## Phase 1: 用語・UI・ブランディング変更

**目的:** 国会特有の用語をすべて川崎市議会の用語に置き換え、ブランドを更新する。DBスキーマ変更は行わない。
**ステータス:** ✅ 完了（2026-02-16）

### Phase 1 実施記録

**検証結果:**
- `pnpm typecheck` — 全パス（web, admin, packages/seed）
- `pnpm lint` — エラーなし（既存の警告2件のみ、今回の変更とは無関係）
- `pnpm test` — 全40テストパス
- 残留用語grep — Phase 1 の漏れなし。残っている「衆議院」「参議院」は Phase 2 対象の型定義ファイルのみ

**変更ファイル数:** 約60ファイル
**主な変更内容:**
1. ブランディング・メタデータ（layout.tsx, manifest.json, login等）
2. トップページ・共通UI（hero, about, team-mirai, logo, footer等）
3. 議案関連コンポーネント（status-progress一院制化, disclaimer, share等）
4. ルート変更（/kokkai/ → /sessions/）
5. 会期・チャット・インタビュー・Admin管理画面
6. 法的ページ（利用規約・プライバシーポリシー）
7. シードデータ（data.ts, bill-contents-data.ts — 川崎市議会の議案内容に全面書き換え）

### 1-1. 用語マッピング

| 現在 | 変更後 |
|------|--------|
| みらい議会 | みらい議会ー川崎版 |
| 国会 | 市議会 / 川崎市議会 |
| 法案 | 議案 |
| 衆議院 / 参議院 | （削除） |
| 国会会期 | 定例会 |
| 法案成立 | 可決 |
| 国会審議中 | 審議中 |
| 国会議事堂 | 川崎市議会 |

### 1-2. ブランディング・メタデータ

| ファイル | 変更内容 |
|----------|----------|
| `web/src/app/layout.tsx` | siteTitle, siteDescription, keywords を川崎版に更新 |
| `web/public/manifest.json` | name, short_name, description |
| `admin/src/app/layout.tsx` | title, description |
| `admin/src/app/login/page.tsx` | ログイン画面テキスト |

### 1-3. トップページ・共通UI（約15ファイル）

| ファイル | 変更内容 |
|----------|----------|
| `web/src/components/top/hero.tsx` | 見出し「いま国会で→いま川崎市議会で」、画像alt |
| `web/src/components/top/about.tsx` | サービス説明文 |
| `web/src/components/top/team-mirai.tsx` | 会派紹介（内容要検討） |
| `web/src/components/top/coming-soon-section.tsx` | 「法案→議案」、外部リンク先を川崎市議会へ |
| `web/src/components/layouts/desktop-menu/logo.tsx` | サービス名・キャッチコピー |
| `web/src/components/layouts/footer/footer.config.ts` | フッターリンク |
| `web/src/config/external-links.ts` | 外部リンクURLを川崎市議会関連に |

### 1-4. 議案関連コンポーネント（約12ファイル）

| ファイル | 変更内容 |
|----------|----------|
| `web/src/features/bills/shared/types/index.ts` | `getBillStatusLabel()` の表示名変更、`HOUSE_LABELS` 暫定対応 |
| `web/src/features/bills/client/components/bill-detail/bill-status-progress.tsx` | ステップを一院制に（上程→委員会→本会議→可決/否決）|
| `web/src/features/bills/client/components/bill-detail/mirai-stance-card.tsx` | 「法案→議案」テキスト |
| `web/src/features/bills/client/components/bill-list/bill-status-badge.tsx` | ステータスラベル |
| `web/src/features/bills/client/components/bill-list/compact-bill-card.tsx` | カードテキスト |
| `web/src/features/bills/server/components/featured-bill-section.tsx` | 「注目の法案→注目の議案」 |
| `web/src/features/bills/server/components/previous-session-section.tsx` | セクションテキスト+リンクパス |
| `web/src/features/bills/client/components/bill-detail/bill-disclaimer.tsx` | 免責事項テキスト |
| `web/src/features/bills/client/components/share/bill-share-modal.tsx` | シェアテキスト |

### 1-5. 会期関連コンポーネント（約4ファイル）

| ファイル | 変更内容 |
|----------|----------|
| `web/src/features/diet-sessions/client/components/current-diet-session.tsx` | 「国会閉会中→閉会中」 |
| `web/src/features/diet-sessions/client/components/diet-session-bill-list.tsx` | テキスト変更 |
| `web/src/features/diet-sessions/client/components/bill-list-with-status-filter.tsx` | フィルターテキスト |

### 1-6. ルート変更

- `web/src/app/kokkai/[slug]/bills/page.tsx` → `web/src/app/sessions/[slug]/bills/page.tsx` に移動
- 関連するリンク先をすべて `/kokkai/` → `/sessions/` に更新

### 1-7. チャット・インタビューUI（約8ファイル）

| ファイル | 変更内容 |
|----------|----------|
| `web/src/features/chat/client/components/chat-window.tsx` | プリセット質問、プレースホルダー |
| `web/src/features/interview-config/client/components/interview-lp-page.tsx` | LP全文 |
| `web/src/features/interview-config/client/components/interview-landing-section.tsx` | テキスト |
| `web/src/features/interview-report/shared/components/report-breadcrumb.tsx` | パンくず |
| `web/src/features/interview-report/shared/components/back-to-bill-button.tsx` | ボタンテキスト |

### 1-8. Admin管理画面（約8ファイル）

| ファイル | 変更内容 |
|----------|----------|
| `admin/src/app/(protected)/layout/navigation-links.tsx` | 「国会会期管理→定例会管理」 |
| `admin/src/features/bills-edit/components/bill-form-fields.tsx` | フォームラベル全般 |
| `admin/src/features/diet-sessions/components/diet-session-form.tsx` | フォームテキスト |
| `admin/src/features/diet-sessions/components/diet-session-item.tsx` | 一覧テキスト+リンク |
| `admin/src/features/diet-sessions/components/diet-session-list.tsx` | ヘッダーテキスト |
| `admin/src/app/(protected)/diet-sessions/page.tsx` | ページタイトル |

### 1-9. 法的ページ

| ファイル | 変更内容 |
|----------|----------|
| `web/src/app/terms/page.tsx` | 利用規約のサービス名・文言 |
| `web/src/app/privacy/page.tsx` | プライバシーポリシーのサービス名 |

### 1-10. シードデータ（テキストのみ）

| ファイル | 変更内容 |
|----------|----------|
| `packages/seed/main/data.ts` | 会期名を「令和X年 第X回定例会」形式に、議案名を川崎市の例に |
| `packages/seed/main/bill-contents-data.ts` | 議案説明文を川崎市議会の文脈に |

### Phase 1 検証チェックリスト

- [ ] `pnpm typecheck` — 型エラーなし
- [ ] `pnpm lint` — フォーマット修正
- [ ] `pnpm test` — テキスト変更に伴うテスト修正
- [ ] `pnpm dev` で全ページ目視確認（`/`, `/bills/[id]`, `/sessions/[slug]/bills`, `/terms`, `/privacy`）
- [ ] Admin（:3001）で議案・定例会管理画面確認
- [ ] 残留チェック: コード全体で「国会」「法案」「衆議院」「参議院」をgrepし漏れがないか確認

---

## Phase 2: DBスキーマ・データモデル変更

**目的:** house_enum削除、テーブル名リネーム、会派見解の汎用化、ステータスenum更新、Supabase型再生成。
**ステータス:** ✅ 完了（2026-02-18 動作確認完了）

### 2-1. マイグレーション①: スキーマ変更

新規ファイル: `supabase/migrations/YYYYMMDD_kawasaki_schema_changes.sql`

```sql
-- 1. house_enum と originating_house カラム削除
DROP INDEX IF EXISTS idx_bills_originating_house;
ALTER TABLE bills DROP COLUMN originating_house;
DROP TYPE house_enum;

-- 2. bills.shugiin_url 削除
ALTER TABLE bills DROP COLUMN shugiin_url;

-- 3. bill_status_enum を市議会用に更新
ALTER TYPE bill_status_enum RENAME TO bill_status_enum_old;
CREATE TYPE bill_status_enum AS ENUM (
    'preparing',        -- 準備中
    'submitted',        -- 上程済み（旧 introduced）
    'in_committee',     -- 委員会審査中（旧 in_originating_house）
    'plenary_session',  -- 本会議採決中（旧 in_receiving_house）
    'approved',         -- 可決（旧 enacted）
    'rejected'          -- 否決
);

-- データ移行
ALTER TABLE bills ALTER COLUMN status TYPE text;
UPDATE bills SET status = CASE status
    WHEN 'introduced' THEN 'submitted'
    WHEN 'in_originating_house' THEN 'in_committee'
    WHEN 'in_receiving_house' THEN 'plenary_session'
    WHEN 'enacted' THEN 'approved'
    ELSE status END;
ALTER TABLE bills ALTER COLUMN status TYPE bill_status_enum USING status::bill_status_enum;
DROP TYPE bill_status_enum_old;

-- 4. diet_sessions → council_sessions リネーム
ALTER TABLE diet_sessions RENAME TO council_sessions;
ALTER TABLE council_sessions RENAME COLUMN shugiin_url TO council_url;
ALTER TABLE bills RENAME COLUMN diet_session_id TO council_session_id;

-- 5. 委員会テーブル追加
CREATE TABLE committees (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name TEXT NOT NULL,
    description TEXT,
    sort_order INTEGER NOT NULL DEFAULT 0,
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
ALTER TABLE committees ENABLE ROW LEVEL SECURITY;
ALTER TABLE bills ADD COLUMN committee_id UUID REFERENCES committees(id);
CREATE INDEX idx_bills_committee_id ON bills(committee_id);
```

### 2-2. マイグレーション②: 会派見解の汎用化

```sql
-- 会派マスターテーブル
CREATE TABLE factions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name TEXT NOT NULL,
    display_name TEXT NOT NULL,
    logo_url TEXT,
    sort_order INTEGER NOT NULL DEFAULT 0,
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
ALTER TABLE factions ENABLE ROW LEVEL SECURITY;

-- 会派見解テーブル（1議案に複数会派の見解を登録可能）
CREATE TABLE faction_stances (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    bill_id UUID NOT NULL REFERENCES bills(id) ON DELETE CASCADE,
    faction_id UUID NOT NULL REFERENCES factions(id) ON DELETE CASCADE,
    type stance_type_enum NOT NULL,
    comment TEXT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    UNIQUE(bill_id, faction_id)
);
ALTER TABLE faction_stances ENABLE ROW LEVEL SECURITY;
CREATE INDEX idx_faction_stances_bill_id ON faction_stances(bill_id);
CREATE INDEX idx_faction_stances_faction_id ON faction_stances(faction_id);

-- mirai_stances からデータ移行後、削除
DROP TABLE mirai_stances;
```

### 2-3. 型再生成 & TypeScript更新

| ファイル | 変更内容 |
|----------|----------|
| `packages/supabase/types/supabase.types.ts` | `pnpm db:types:gen` で再生成 |
| `web/src/features/bills/shared/types/index.ts` | HouseEnum削除、BillStatusEnum更新、FactionStance型追加 |
| `admin/src/features/bills-edit/types/index.ts` | Zodスキーマ更新（originating_house削除、status値更新） |

### 2-4. ディレクトリ・ファイルリネーム

| 変更前 | 変更後 |
|--------|--------|
| `web/src/features/diet-sessions/` | `web/src/features/council-sessions/` |
| `admin/src/features/diet-sessions/` | `admin/src/features/council-sessions/` |
| `admin/src/features/mirai-stance/` | `admin/src/features/faction-stances/` |

### 2-5. データローダー更新（約10ファイル）

| ファイル | 変更内容 |
|----------|----------|
| `web/src/features/bills/server/loaders/get-bill-by-id.ts` | `mirai_stances` → `faction_stances` + `factions` JOIN |
| `web/src/features/bills/server/loaders/get-featured-bills.ts` | 同上 |
| `web/src/features/bills/server/loaders/get-bills-by-diet-session.ts` | リネーム+テーブル名更新 |
| `web/src/features/bills/server/loaders/get-coming-soon-bills.ts` | originating_house削除 |
| Admin側の全ローダー・アクション | テーブル名更新 |

### 2-6. UIコンポーネント更新

| ファイル | 変更内容 |
|----------|----------|
| `bill-status-progress.tsx` | 新ステータスenum対応、originatingHouse prop削除 |
| `mirai-stance-card.tsx` → `faction-stance-card.tsx` | 複数会派表示対応 |
| Admin議案フォーム | originating_house フィールド削除、会派選択UI追加 |

### 2-7. シードデータ完全更新

| ファイル | 変更内容 |
|----------|----------|
| `packages/seed/main/data.ts` | 全テーブル参照名更新、会派データ追加 |
| `packages/seed/main/run.ts` | 挿入順序更新（factions → council_sessions → bills → faction_stances） |

### Phase 2 実施記録

**コード変更:**
1. マイグレーション `supabase/migrations/20260216180000_kawasaki_schema_changes.sql` 作成・適用確認済み
2. `packages/supabase/types/supabase.types.ts` 型再生成済み
3. TypeScript型定義更新（BillStatusEnum, FactionStance型, HouseEnum削除）
4. ディレクトリリネーム（diet-sessions→council-sessions, mirai-stance→faction-stances）
5. ファイル名・型名・関数名・変数名の全リネーム（DietSession→CouncilSession等）
6. データローダー・UIコンポーネント・キャッシュタグの全更新
7. シードデータ全面書き換え（factions, committees, council_sessions対応）

**変更ファイル数:** 約50ファイル

### Phase 2 検証チェックリスト

- [x] `supabase start` でマイグレーション実行確認（全36マイグレーション成功）
- [x] `pnpm db:types:gen` で型生成済み
- [x] `pnpm typecheck` — 全パス（web, admin, packages/seed）
- [x] `pnpm test` — 全40テスト通過
- [x] `pnpm seed` — 正常完了（council_sessions×2, committees×4, factions×3, bills×9, faction_stances×9）
- [x] `pnpm dev` + Admin動作確認 — http://localhost:3001 で 307 → ログイン画面表示OK
- [x] Web動作確認 — http://localhost:3002 で 200、/sessions/r7-4/bills・/bills/{id} とも正常表示

---

## Phase 3: AI機能・コンテンツ適応

**目的:** AIチャット・インタビューのプロンプトを川崎市議会文脈に更新。必要に応じて新機能追加。
**ステータス:** ✅ 完了（2026-02-18）

### 3-1. AIチャットプロンプト

| ファイル | 変更内容 |
|----------|----------|
| `web/src/features/chat/server/services/handle-chat-request.ts` | システムプロンプトを川崎市議会文脈に |
| Langfuse上のプロンプトテンプレート | 環境変数で管理されている場合は更新 |

### 3-2. インタビューAIプロンプト（約4ファイル）

| ファイル | 変更内容 |
|----------|----------|
| `web/src/features/interview-session/server/utils/build-interview-system-prompt.ts` | 「法案→議案」、市民生活への影響を重視 |
| `web/src/features/interview-session/server/utils/interview-logic/loop-mode.ts` | プロンプト文言 |
| `web/src/features/interview-session/server/utils/interview-logic/bulk-mode.ts` | プロンプト文言 |

### 3-3. 川崎市議会固有の拡張（任意・将来）

- 委員会管理ページ（Admin）
- 委員会別フィルタリング（Web）
- 川崎市議会データのインポートツール
- 川崎市固有のタグ（まちづくり、子育て支援、防災等）

### Phase 3 実施記録

**変更ファイル:**
1. `web/src/features/interview-session/server/utils/build-interview-system-prompt.ts` — 法案情報→議案情報、法案検討→議案検討、社会的影響→市民生活・行政運営への影響
2. `web/src/features/interview-session/server/utils/interview-logic/loop-mode.ts` — 法案情報→議案情報、法案に関する質問→議案に関する質問
3. `web/src/features/interview-session/server/utils/interview-logic/bulk-mode.ts` — 同上（nextQuestion専用プロンプトと通常プロンプト両方）
4. `web/src/features/interview-session/shared/schemas.ts` — スコアのdescription、stanceのdescription更新

**AIチャット（Langfuse管理）について:**
- `top-chat-system` と `bill-chat-system-{difficultyLevel}` の2種のプロンプトはLangfuseで管理
- ローカル環境ではLangfuse APIキー未設定のためチャット機能は動作しないが、コード側の変更は不要（プロンプトはLangfuse側で更新が必要）

### Phase 3 検証チェックリスト

- [x] `pnpm typecheck` — 全パス
- [x] `pnpm test` — 全40テスト通過
- [x] ソースコード全体に「法案」の残存なし（grep 0件）
- [ ] AIチャットで川崎市議会に関する質問をして適切な回答が返ることを確認（Langfuse設定後）
- [ ] インタビューフロー全体を通してテスト（本番環境またはLangfuse設定後）

---

## 実施順序と依存関係

```
Phase 1（用語・UI）← 先行して着手可能
    ↓
Phase 2（DBスキーマ）← Phase 1の用語統一が前提
    ↓
Phase 3（AI機能）← Phase 2の新スキーマ型が前提
```

## リスク軽減策

| リスク | 対策 |
|--------|------|
| 用語漏れ | 各Phase完了後に `国会`, `法案`, `衆議院`, `参議院`, `diet_session`, `mirai_stance` をgrep |
| マイグレーション失敗 | ローカルSupabaseで十分にテストしてからコミット |
| インポート壊れ | ディレクトリリネーム後は `pnpm typecheck` で全import確認 |
| AIプロンプト品質 | 手動QAで川崎市議会文脈の回答品質を検証 |

---

---

## Phase 4: 品質改善・本番公開準備

**目的:** QAテスト結果を受けたバグ修正、AI機能のコスト管理、チームみらい表記の設定化（非公式公開対応）、FAQ・帰属表記の整備。
**ステータス:** ✅ 完了（2026-03-05）

### 4-1. AI機能のオン/オフ設定

| ファイル | 変更内容 |
|----------|----------|
| `web/src/config/site.config.ts` | `features.aiChat`, `features.aiInterview` フラグ追加 |
| `web/src/components/layouts/main-layout.tsx` | aiChat オフ時にサイドバーオフセットを除去 |
| `web/src/features/bills/client/components/bill-detail/bill-detail-client.tsx` | aiChat オフ時はテキスト選択ラッパーを無効化 |
| `web/src/features/bills/client/components/bill-detail/long-press-section.tsx` | aiChat オフ時は非表示 |
| `web/src/features/bills/server/components/bill-detail/bill-detail-layout.tsx` | aiInterview オフ時はDBクエリもスキップ |
| `web/src/app/page.tsx` | aiChat オフ時はHomeChatClientを非表示 |

### 4-2. バグ修正（QAテスト対応）

| バグ | ファイル | 内容 |
|------|----------|------|
| A-2-6 | `council-session-bill-list.tsx` | 定例会外部リンクがsiteConfig固定値になっていた → `session.council_url` に修正 |
| B-3-5 | `admin/bills-edit/` 5ファイル | Admin議案フォームに委員会選択UIが欠如 → Select追加、`__none__` センチネル対応 |
| バッジ | `bill-status-badge.tsx` | 「審議中」統一ラベルを「委員会審査中」「本会議採決中」等に分離 |
| AI | `handle-interview-chat-request.ts` | `AI_TypeValidationError` でcrashしていた → try-catch + currentStageフォールバック |
| チャットログ | `report-chat-log-page.tsx` | アシスタントメッセージがJSON文字列のまま表示 → `text` フィールドを抽出して表示 |
| 開会中判定 | `get-current-council-session.ts` | `end_date IS NULL`（終了日未定）の会期が閉会中と判定 → `.or("end_date.gte.{date},end_date.is.null")` に修正 |

### 4-3. チームみらい表記の設定化（非公式公開対応）

非公式運営で公開する際に党公式と誤解されないよう、全表記を `site.config.ts` で制御できるように変更。

| 追加設定 | 説明 |
|----------|------|
| `features.showTeamMiraiSection` | チームみらいロゴ・リンクセクションの表示制御 |
| `managingParty` | 管理政党名（空文字列なら汎用表現にフォールバック） |
| `operator.name` | 運営者名（利用規約・FAQ・帰属表記に使用） |
| `operator.contactUrl` | 問い合わせ先URL |
| `operator.jurisdiction` | 利用規約の管轄裁判所 |

対応箇所：ヘッダーロゴ・フッターロゴ・コピーライト・トップページ powered by テキスト・AIインタビュー各テキスト・利用規約・プライバシーポリシー

### 4-4. FAQページ・議案URLの整備

| 変更内容 | 詳細 |
|----------|------|
| FAQページ新設（`/faq`） | チームみらいNotionへの外部リンクをプロジェクト内ページに切り替え |
| About帰属・免責表記 | `showTeamMiraiSection: false` 時に表示。チームみらい/本家みらい議会へのリンク、非公式サイト宣言、開発者連絡先 |
| 議案一覧URL修正 | 川崎市議会公式の正しいURL（`/shisei/category/40-7-25-...`）に変更。`councilBillsUrl` を廃止し `councilBillsDetailUrl` に一本化 |

### Phase 4 検証チェックリスト

- [x] `pnpm typecheck` — 全パス
- [x] `pnpm lint` — エラーなし
- [x] 手動QA 61項目 — 全OK
- [x] AI機能オフ時のUI確認（レイアウト崩れなし）
- [x] チームみらい表記 → site.config参照に全変換

---

## 進捗ログ

| 日時 | 内容 |
|------|------|
| 2026-02-16 | 計画策定完了 |
| 2026-02-16 | Phase 1 完了（約60ファイル変更） |
| 2026-02-17 | Phase 2 コード変更完了（約50ファイル）。typecheck・test全パス。ローカルDocker未構築のためseed/dev動作確認は保留 |
| 2026-02-18 | Docker Desktop導入後、supabase start・pnpm seed・pnpm dev動作確認完了。Phase 2 全チェックリスト通過。Phase 3 着手へ |
| 2026-02-18 | Phase 3 完了。インタビュープロンプト4ファイルの「法案」→「議案」置換、市議会文脈への更新完了。typecheck・test全パス |
| 2026-03-05 | Phase 4 完了。QAバグ修正・AI機能フラグ・チームみらい表記設定化・FAQ新設・議案URL修正。本番公開準備完了 |
